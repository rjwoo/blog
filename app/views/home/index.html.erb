<div class="header">
  <h1>The Fantasy Corner</h1>
</div>
<% @posts.each do |post| %>
  <h2><%= link_to post.title, post_path(post) %></h2>
  <%= link_to "Delete", post_path(post), method: :delete, data: {confirm: "Are you sure?"} %>
  <hr>
<% end %>

<%= paginate @posts %>
